# notebook_setup.py
import os
import sys

sys.path.append(os.path.abspath(".."))

# sets up the path so that the workspace libraries are accessible
# then, in future just place the following line in each notebook
# %run ../notebook_setup.py
